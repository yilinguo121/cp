#include <bits/stdc++.h>
using namespace std;
#define IO ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
const long long INF = numeric_limits<int>::max();
int main() {
	int n;
	cin >> n;
	string s, t;
	cin >> s >> t;
	vector<vector<vector<int>>> dp(n, vector<int>(n, vector<int>(2)));
	for (int i = 0;i < n;i++) {
		dp[		dp[]]
	}
}

